import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.hoonkim.inventorymanager',
  appName: 'mmt-dev-assignmnet',
  webDir: 'dist'
};

export default config;
