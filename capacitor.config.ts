import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.hoonkim.inventorymanager',
  appName: 'inventory-manager-interface',
  webDir: 'dist'
};

export default config;
